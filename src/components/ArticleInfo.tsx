import React, { type FC } from 'react'

const f = '⇒ ArticleInfo.tsx (ArticleInfo):'

const ArticleInfo: FC = () => {
  return (
    <div>
      <h1>ArticleInfo</h1>
    </div>
  )
}
export default ArticleInfo
